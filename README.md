# mdns-zig

std-only mDNS (RFC 6762) and DNS-SD (RFC 6763) for Zig.

## Installation

```sh
zig fetch --save https://github.com/nullstyle/mdns-zig/archive/refs/tags/v0.1.0.tar.gz
```

The build option map is `{target, optimize}`.
